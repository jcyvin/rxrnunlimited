{
  "name": "rxrn-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npx vercel dev --listen 3000",
    "preview": "npx vercel dev",
    "deploy": "npx vercel --prod",
    "server": "node server.js"
  }
  ,"dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.7"
  }
}
